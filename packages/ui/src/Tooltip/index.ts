/**
 * Expose the shared Tooltip through the application UI package.
 * Call sites should import from `@pure/ui`; swap for a local implementation later.
 */
export { Tooltip, type TooltipProps } from '@lobehub/ui'
