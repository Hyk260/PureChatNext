/**
 * Expose the shared Avatar component through the application UI package.
 * Call sites should import from `@pure/ui`; swap for a local implementation later.
 */
export { Avatar } from '@lobehub/ui'
