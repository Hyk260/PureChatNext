import { Switch } from 'antd'
import type { SwitchProps } from 'antd'

/** Stable switch entrypoint for application call sites. */
export type SwitchSize = NonNullable<SwitchProps['size']>

export { Switch, type SwitchProps }
