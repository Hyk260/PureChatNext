/**
 * Expose the shared Modal through the application UI package.
 * Call sites should import from `@pure/ui`; swap for a local implementation later.
 */
export { Modal, confirmModal } from '@lobehub/ui/base-ui'
