# Agents 入库 · Implementation Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 助理列表入库；系统内置 Pure AI + 用户自建 CRUD；移除 `HOME_AGENTS` 硬编码。

**Architecture:** Drizzle `agents` 表；API 仿 knowledge-bases；侧栏/首页改 fetch；继续 `?agent=` + `chat_topics`。

**Tech Stack:** Drizzle、Better Auth、Next.js App Router、Vitest、`@lobehub/ui`。

**Spec:** `docs/superpowers/specs/2026-07-15-agents-db-design.md`

---

## File map

| File | Responsibility |
|------|----------------|
| `src/database/schemas/agent.ts` | `agents` 表 |
| `src/database/schemas/index.ts` | 导出 |
| `drizzle/` migration | 含 Pure AI seed |
| `src/const/home/agents.ts` | 仅 `PURE_AI_AGENT_ID` / 默认 meta |
| `src/database/models/agent.ts` | AgentModel |
| `src/database/models/agent.test.ts` | 单测 |
| `src/app/api/agents/route.ts` | GET list / POST |
| `src/app/api/agents/[id]/route.ts` | GET / PATCH / DELETE |
| `src/features/home/.../AgentSection.tsx` 等 | 列表 + CRUD UI |
| `src/features/chat/ChatPage.tsx` | agent 详情兜底 |

---

### Task 1: Spec / Plan / Phase2 回写

- [x] 写 design spec 与本 plan
- [x] Phase2 spec 标注 agents 表已由本规格取代

### Task 2: Schema + migration + seed

- [x] `src/database/schemas/agent.ts`
- [x] schemas index 导出
- [x] migration + Pure AI `INSERT ON CONFLICT DO NOTHING`
- [x] `chat.ts` 注释约定 `agent_id` = `agents.id`

### Task 3: Model + tests

- [x] `AgentModel(userId)`: listVisible, create, findVisibleById, update, delete, ensureBuiltin
- [x] Vitest：可见性、内置不可删、有 topic 不可删

### Task 4: API routes

- [x] `/api/agents` GET/POST
- [x] `/api/agents/[id]` GET/PATCH/DELETE（403/404/409）

### Task 5: Frontend list + CRUD UI

- [x] agentApi client
- [x] AgentSection / HomeAgentSelect / HomeChatInput
- [x] 新建 / 编辑 / 删除 UI
- [x] ChatPage + store 默认 `agt_inbox`

### Task 6: Cleanup

- [x] 移除 `HOME_AGENTS` 数组与 `findHomeAgent`
- [x] 冒烟：列表 / CRUD / 发消息带 systemRole

---

## Spec coverage

| Spec | Task |
|------|------|
| A1 Schema | 2 |
| A2 Model | 3 |
| A3 API | 4 |
| A4 列表 | 5 |
| A5 CRUD UI | 5 |
| A6 常量 | 6 |
