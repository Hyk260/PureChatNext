{
  "name": "@pure/ssrf-safe-fetch",
  "version": "1.0.0",
  "private": true,
  "description": "PureChatNext SSRF-safe fetch (Node + browser conditional exports)",
  "exports": {
    ".": {
      "browser": "./index.browser.ts",
      "node": "./index.ts",
      "default": "./index.ts"
    }
  },
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest --coverage --silent='passed-only'"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "request-filtering-agent": "^3.0.2"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
