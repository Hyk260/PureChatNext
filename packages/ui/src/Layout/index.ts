/**
 * Expose shared layout primitives through the application UI package.
 * Call sites should import from `@pure/ui`; swap for a local implementation later.
 */
export { Center, Grid, MaskShadow, ScrollShadow } from '@lobehub/ui'
