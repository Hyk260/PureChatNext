/**
 * Expose the shared MaterialFileTypeIcon through the application UI package.
 * Call sites should import from `@pure/ui`; swap for a local implementation later.
 */
export { MaterialFileTypeIcon } from '@lobehub/ui'
