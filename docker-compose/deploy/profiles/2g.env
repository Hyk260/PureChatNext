# 默认资源档案。由 setup-deploy / install.sh 写入 .env。
# 生产只运行应用容器；PostgreSQL 与 Redis 使用宿主机已有实例。
# 应用内存上限 512m。

USE_CN_MIRROR=true
NODE_MAX_OLD_SPACE_SIZE=320
APP_MEMORY_LIMIT=512m
APP_CPUS=1.0
APP_PIDS_LIMIT=256
