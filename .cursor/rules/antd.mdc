---
description: Ant Design（antd v6）组件用法与本仓库约定；编写 antd / antd-style 代码时启用
globs: "**/*.{tsx,ts}"
alwaysApply: false
---

# Ant Design

Read https://ant.design/llms-full.txt and understand Ant Design components. Use this knowledge when writing code with Ant Design.

单组件文档：`https://ant.design/components/<name>.md`（中文加 `-cn`，如 `button-cn.md`）。
导航索引：https://ant.design/llms.txt  
Agent 指引：https://ant.design/docs/react/for-agents.md

本仓库依赖：`antd` ^6.x；样式常用 `antd-style`（`createStaticStyles`、`cssVar`、`cx`）。

## 编写约定

- 从 `antd` 具名导入组件；类型优先用 `GetProps` / `GetRef` / `GetProp`（`import type { GetProps } from 'antd'`），与现有代码风格一致。
- 布局优先 `Flex`（本仓库已广泛使用），其次 `Space`；避免随意引入整套 `Layout` 脚手架。
- 反馈类 API：**优先** `App.useApp()` 取 `message` / `modal` / `notification`，勿用已弃用的静态 `message.xxx`（主题/上下文会错）。全局静态桥接见 `src/components/AntdStaticMethods`。
- `ConfigProvider` 与 `App` 成对：主题 token 在 `ConfigProvider` 内，`App` 包在其内层；本仓库主题入口见 `src/layout/ThemeProviders.tsx`。
- Button：主操作 `type="primary"`；危险操作用 `danger`；可用 `color` + `variant` 组合变体（v5.21+ / v6）。
- 定制样式优先 Design Token / `classNames` / `styles`（语义化 DOM），再配合 `antd-style`；避免硬编码与 antd 内部类名耦合。
- API 不确定时先查对应 `components/<name>.md` 或 `llms-full.txt`，勿臆造已移除的 v3/v4 API。
