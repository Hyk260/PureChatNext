/**
 * Expose the shared Icon component through the application UI package.
 * Call sites should import from `@pure/ui`; swap for a local implementation later.
 */
export { Icon, type IconProps } from '@lobehub/ui'
