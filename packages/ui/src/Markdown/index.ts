/**
 * Bridge over `@lobehub/ui` content renderers.
 * Call sites should import from `@pure/ui`; swap for a local implementation later.
 */
export { FileTypeIcon, Highlighter, Markdown, type MarkdownProps } from '@lobehub/ui'
