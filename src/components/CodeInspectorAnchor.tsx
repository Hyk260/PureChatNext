'use client'

/**
 * Stable client entry for code-inspector-plugin (`injectTo`).
 * Keeps inspector UI / hotkeys alive across routes and HMR.
 * @see https://inspector.fe-dev.cn/en/api/advance.html#injectto
 */
export default function CodeInspectorAnchor() {
  return null
}
